; V5 (TUHS Dennis_v5) - one RK05 root disk on a PDP-11/45.
; Boot: `boot rk0`, then `unix` at the `@` prompt, `root` at `login:`.
set cpu 11/45
set cpu idle
attach rk0 ../images/v5root
set console telnet=10023
boot rk0
