# downloaded disk images, tapes, compressed sources, and simh-created
# device placeholders (git-ignored - refetched on demand)
images/*

# python
__pycache__/
*.pyc

# autotools build artifacts (junk from running ./configure)
# configure, Makefile.in and Makefile are tracked (./configure && make works)
config.log
config.status
autom4te.cache/
aclocal.m4
