prebsd fetches and runs Research Unix V4, V5, V6, V7 and 32V on SIMH (the
pdp11 and vax780 simulators), driving the console over telnet.

It ships `fetch` (download/unpack a disk image from the images.json manifest)
and `boot.py` (the console driver).
