# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines of text following the handy ruler.

prebsd: prebsd (fetch and run Research Unix V4-V7 and 32V on SIMH)
prebsd:
prebsd: prebsd fetches and runs Research Unix V4, V5, V6, V7 and 32V on
prebsd: SIMH (the pdp11 and vax780 simulators), driving the console over
prebsd: telnet. It ships fetch (download a disk image from images.json)
prebsd: and boot.py (the console driver).
prebsd:
prebsd: Homepage: https://github.com/moebiusV/prebsd
prebsd:
prebsd:
prebsd:
